defmodule DianBot.Commands do
  @moduledoc """
  Slash command framework for group-message bot events.

  Commands are parsed from normalized message segments and dispatched to
  registered handlers or batch workflows.
  """
end
